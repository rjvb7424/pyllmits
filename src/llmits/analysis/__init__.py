"""Everything that turns a finished (or resumed) run into artifacts.

    plots    results.json -> summary tables + matplotlib charts
    viewer   results.json -> interactive viewer.html replay
    videos   in-memory frames -> one mp4 per model
"""
